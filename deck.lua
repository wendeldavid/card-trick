local suits = require("suits")

local allCards = {}

local populate = function()
 --popular todas as cartas de um baralho 
end

local runCycle = function()
    for cycle = 1, 3 do
        --distribuir todas as cartas em colunas
        --ler a escolha do usuario
        --reunificar as colunas ordenadas em uma unica lista
        cycle = cycle + 1
    end
end